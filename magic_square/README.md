# 🪄 Magic Square Generator using Python (GUI)
This project is a Graphical User Interface (GUI) based **Magic Square Generator** developed using Python and Tkinter.  
It generates a magic square for any **odd number** and displays it in a **grid interface (like Tic Tac Toe)**.

## 🖼️ Demo
<img src="magic_square_demo.gif" width="400">

## 🎯 Features
* User-friendly grid interface  
* Generates magic square for odd numbers  
* Displays result in matrix form  
* Popup error for even numbers  
* Clean and attractive UI  

## 🧠 Game Logic
The program uses the **Siamese Method** to generate a magic square.
The sum of every row, column, and diagonal is the same.
  
## ▶️ How to Run
* Step 1: Install Python
Make sure Python is installed:

* python --version
Step 2: Run the program
python magic_square_gui.py

## 🛠️ Technologies Used
Python 3
Tkinter (GUI library)